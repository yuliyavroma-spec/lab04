name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install CMake
      run: sudo apt-get update && sudo apt-get install -y cmake
      
    - name: Configure
      run: cmake -B build -DCMAKE_INSTALL_PREFIX=install
      
    - name: Build
      run: cmake --build build
      
    - name: Install
      run: cmake --build build --target install

  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Configure
      run: cmake -B build -DCMAKE_INSTALL_PREFIX=install
      
    - name: Build
      run: cmake --build build
      
    - name: Install
      run: cmake --build build --target install
